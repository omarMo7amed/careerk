export type FormData = {
  email: string;
  password: string;
  remember: boolean;
};

export type Errors = {
  email: string;
  password: string;
};

export type MatchItemProps = {
  emoji: string;
  title: string;
  company: string;
  score: string;
};
